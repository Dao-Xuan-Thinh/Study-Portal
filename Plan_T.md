(Unfinish Do not do stuff in here)

Bug Fix
1. keywords explaination doesnt work (make the keywords highlight in yellow for easier access)
2. Chapter 17 legacy mode has alot of errors
    one example:{
                    ≡ƒôÉ Amdahl's Law for SMP Speedup Limit
                    // Speedup with N processors
                    Speedup(N) =        1
                                ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
                                (1 - f) + (f / N)

                    f = fraction of code parallelizable
                    N = number of processors
                    (1-f) = sequential fraction (bottleneck)
                }







QOL
1. (Your idea) Floating in-page TOC: sidebar that shows section headings for current chapter, highlights active section on scroll